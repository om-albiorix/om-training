// use to array comparing 
const myAwesomeArray = [5, 4, 3, 2, 1];
const myFunc=(myAwesomeArray)=>myAwesomeArray <10;
let  y=myAwesomeArray.every(myFunc);
console.log(y)
// it return in boolean true or false