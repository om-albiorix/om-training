// string to number
Number("om");
// it return NaN;

let m = "5";
let x = +m;
console.log(typeof x);

// to exponential always return string
// to fiexed always return string
// to precision always return string

let x1 = new Date();
String(x1);
typeof (x1);
